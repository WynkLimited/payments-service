spring:
  port: 8082
  application:
    name: paymentApi
  profiles:
    active: prod
  cloud:
    config:
      uri: http://mac.local:8081
      name: subscription,subscription-core,common
      label: prod

logging:
  level:
    root: DEBUG
